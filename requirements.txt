# Зависимости решения «Фенолог».
#
# Версии зафиксированы точно, а не диапазоном: критерий воспроизводимости
# требует, чтобы эксперт получил тот же результат, что и мы, а не тот, что
# соберётся у него сегодня.
#
# Числа взяты из рабочего окружения, а не выписаны по памяти. Раньше здесь
# стояли версии на два-три релиза свежее установленных (fastapi 0.141 против
# фактических 0.115), то есть файл закреплял набор, который никогда не
# запускался, — ровно тот способ уронить сборку в момент проверки.
#
# Из списка сознательно убраны geopandas, odc-stac и rioxarray: их не
# импортирует ни один модуль репозитория, а тянут они за собой GDAL, xarray и
# dask — минуты сборки образа и сотни мегабайт впустую.

# --- Доменное ядро: восстановление ряда, норма, поиск аномалий ---
numpy==2.5.1
pandas==3.0.5
scipy==1.18.1
scikit-learn==1.9.0
lightgbm==4.7.0

# --- Веб-сервис ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.3

# --- Автоматический сбор данных ---
# rasterio читает COG прямо из облачного хранилища по диапазону байт, поэтому
# сцена целиком не скачивается; planetary-computer подписывает ссылки на активы.
requests==2.34.2
shapely==2.1.2
pyproj==3.7.2
rasterio==1.5.1
pystac-client==0.9.0
planetary-computer==1.0.0

# --- Графики ---
# Нужен и сервису: на matplotlib рисуются графики клиентского PDF-отчёта
# (`src/reporting/charts.py`), а также картинки исследовательского отчёта
# (`python notebooks/figures.py`).
matplotlib==3.11.1

# --- Клиентский PDF-отчёт ---
# Чистый Python: документ собирается на сервере без браузера и без системных
# библиотек, поэтому в контейнере ничего доустанавливать не нужно. Кириллический
# шрифт берётся из matplotlib, отдельного пакета со шрифтами не требуется.
reportlab==5.0.1
